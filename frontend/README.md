# Frontend React (legado)

Este frontend React + Vite foi substituído pelo módulo **`petshop-web`** (Spring Boot + Thymeleaf).

Use o painel em: **http://localhost:8090** (após subir o `petshop-web`).

Instruções completas na raiz do projeto: `README.md`.
